<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>R. Jagathsingh Raja</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Jost:wght@300;400;500&display=swap" rel="stylesheet"/>
<style>
:root {
  --bg: #faf9f7;
  --bg-off: #f3f1ee;
  --bg-card: #ffffff;
  --ink: #1a1814;
  --ink-mid: #5a5650;
  --ink-light: #9a9490;
  --ink-faint: #d8d4cf;
  --accent: #b5936a;
  --accent-pale: #f0e8df;
  --serif: 'Cormorant Garamond', Georgia, serif;
  --sans: 'Jost', sans-serif;
  --maxw: 960px;
  --r: 8px;
  --ease: cubic-bezier(.4,0,.2,1);
  --spring: cubic-bezier(.34,1.56,.64,1);
  --out: cubic-bezier(.16,1,.3,1);
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  background: var(--bg);
  color: var(--ink);
  font-family: var(--sans);
  font-weight: 300;
  line-height: 1.75;
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
}
a { text-decoration: none; color: inherit; }
ul { list-style: none; }
em { font-style: italic; }

/* CUSTOM CURSOR */
#cdot {
  width: 6px; height: 6px;
  background: var(--ink);
  border-radius: 50%;
  position: fixed;
  top: 0; left: 0;
  pointer-events: none;
  z-index: 9999;
  transform: translate(-50%, -50%);
  transition: opacity .2s, background .2s;
}
#cring {
  width: 32px; height: 32px;
  border: 1.5px solid var(--accent);
  border-radius: 50%;
  position: fixed;
  top: 0; left: 0;
  pointer-events: none;
  z-index: 9998;
  transform: translate(-50%, -50%);
  transition: width .3s var(--spring), height .3s var(--spring), border-color .25s;
}
#cring.big { width: 48px; height: 48px; border-color: var(--ink); }
#cdot.faded { opacity: 0; }

/* PROGRESS BAR */
#pbar {
  position: fixed;
  top: 0; left: 0;
  height: 2px; width: 0;
  background: var(--accent);
  z-index: 999;
}

/* SCROLL REVEAL */
.reveal {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity .7s var(--out), transform .7s var(--out);
}
.reveal.show { opacity: 1; transform: none; }
.delay1 { transition-delay: .1s; }
.delay2 { transition-delay: .2s; }
.delay3 { transition-delay: .3s; }
.delay4 { transition-delay: .4s; }
.delay5 { transition-delay: .5s; }

/* KEYFRAMES */
@keyframes fadeDown { from { opacity:0; transform:translateY(-14px); } to { opacity:1; transform:none; } }
@keyframes fadeUp   { from { opacity:0; transform:translateY(20px); }  to { opacity:1; transform:none; } }
@keyframes growLine { from { width:0; } to { width:100%; } }
@keyframes scrollWipe {
  0%   { transform:scaleY(0); transform-origin:top; }
  49%  { transform:scaleY(1); transform-origin:top; }
  50%  { transform:scaleY(1); transform-origin:bottom; }
  100% { transform:scaleY(0); transform-origin:bottom; }
}
@keyframes pulseGold {
  0%,100% { box-shadow: 0 0 0 0 rgba(181,147,106,.5); }
  50%      { box-shadow: 0 0 0 7px rgba(181,147,106,0); }
}
@keyframes bobble {
  0%,100% { transform: translateY(0) scale(1.3) rotate(-6deg); }
  50%      { transform: translateY(-6px) scale(1.3) rotate(-6deg); }
}

/* NAV */
#nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.2rem 3rem;
  background: rgba(250,249,247,.92);
  backdrop-filter: blur(14px);
  border-bottom: 1px solid transparent;
  transition: border-color .4s, box-shadow .4s, padding .3s;
}
#nav.scrolled {
  border-color: var(--ink-faint);
  box-shadow: 0 2px 20px rgba(0,0,0,.05);
  padding-top: .85rem;
  padding-bottom: .85rem;
}
.nlogo {
  font-family: var(--serif);
  font-size: 1.35rem;
  font-weight: 600;
  letter-spacing: .05em;
  animation: fadeDown .5s var(--ease) .1s both;
}
.nlinks { display: flex; gap: 2rem; }
.nlinks li { animation: fadeDown .5s var(--ease) both; }
.nlinks li:nth-child(1) { animation-delay: .15s; }
.nlinks li:nth-child(2) { animation-delay: .20s; }
.nlinks li:nth-child(3) { animation-delay: .25s; }
.nlinks li:nth-child(4) { animation-delay: .30s; }
.nlinks li:nth-child(5) { animation-delay: .35s; }
.nlinks li:nth-child(6) { animation-delay: .40s; }
.nlinks li:nth-child(7) { animation-delay: .45s; }
.nlinks li:nth-child(8) { animation-delay: .50s; }
.nlinks li:nth-child(9) { animation-delay: .55s; }
.nlinks a {
  font-size: .73rem;
  font-weight: 400;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--ink-mid);
  position: relative;
  padding-bottom: 3px;
  transition: color .25s;
}
.nlinks a::after {
  content: '';
  position: absolute;
  bottom: 0; left: 0;
  width: 0; height: 1px;
  background: var(--accent);
  transition: width .3s var(--ease);
}
.nlinks a:hover, .nlinks a.active { color: var(--ink); }
.nlinks a:hover::after, .nlinks a.active::after { width: 100%; }

/* HERO */
#hero {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 0 3rem 5rem;
  max-width: var(--maxw);
  margin: 0 auto;
  scroll-margin-top: 70px;
  position: relative;
}

    /* HERO PHOTO */
    .hero-photo {
      width: 110px;
      height: 110px;
      border-radius: 50%;
      object-fit: cover;
      object-position: center top;
      margin-bottom: 1.75rem;
      display: block;
      border: 2px solid var(--ink-faint);
      filter: grayscale(100%);
      animation: fadeUp .7s var(--out) .1s both;
      transition: border-color .3s, transform .35s var(--spring);
    }
    .hero-photo:hover {
      border-color: var(--accent);
      transform: scale(1.04);
    }

    .h-eyebrow {
  font-size: .75rem;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 1.25rem;
  animation: fadeUp .7s var(--out) .2s both;
}
.h-name {
  font-family: var(--serif);
  font-size: clamp(2.5rem, 5.5vw, 5.8rem);
  font-weight: 300;
  line-height: 1;
  letter-spacing: -.01em;
  margin-bottom: 1.75rem;
  white-space: nowrap;
  animation: fadeUp .9s var(--out) .35s both;
  transition: color .3s;
}
.h-name:hover { color: var(--accent); }
.h-name em { font-style: italic; color: var(--ink-mid); }
.h-tagline {
  font-size: .95rem;
  color: var(--ink-mid);
  font-weight: 300;
  margin-bottom: 2rem;
  animation: fadeUp .7s var(--out) .55s both;
}
.h-links {
  display: flex;
  align-items: center;
  gap: .75rem;
  flex-wrap: wrap;
  animation: fadeUp .7s var(--out) .7s both;
}
.h-links a {
  font-size: .82rem;
  color: var(--ink-mid);
  border-bottom: 1px solid var(--ink-faint);
  padding-bottom: 1px;
  transition: color .25s, border-color .25s;
}
.h-links a:hover { color: var(--accent); border-color: var(--accent); }
.hdiv { color: var(--ink-faint); }
.h-line {
  width: 0;
  height: 1px;
  background: var(--ink-faint);
  margin-top: 4rem;
  animation: growLine 1.2s var(--ease) .9s both;
}
.h-scroll-hint {
  position: absolute;
  right: 3rem; bottom: 5rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .75rem;
  animation: fadeUp .6s var(--ease) 1.1s both;
}
.h-scroll-hint span {
  font-size: .62rem;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: var(--ink-light);
  writing-mode: vertical-rl;
}
.scroll-line {
  width: 1px; height: 48px;
  background: var(--ink-faint);
  overflow: hidden;
  position: relative;
}
.scroll-line::after {
  content: '';
  position: absolute; inset: 0;
  background: var(--accent);
  animation: scrollWipe 2s ease-in-out 1.3s infinite;
}

/* SECTIONS */
section {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 5.5rem 3rem;
  scroll-margin-top: 70px;
}
.sm {
  font-family: var(--serif);
  font-size: .72rem;
  letter-spacing: .18em;
  color: var(--ink-faint);
  margin-bottom: .5rem;
}
.sh {
  font-family: var(--serif);
  font-size: clamp(2rem, 4vw, 3rem);
  font-weight: 300;
  letter-spacing: -.01em;
  color: var(--ink);
  margin-bottom: 3rem;
  line-height: 1.1;
  display: inline-block;
  position: relative;
}
.sh::after {
  content: '';
  position: absolute;
  bottom: -6px; left: 0;
  width: 0; height: 1px;
  background: var(--accent);
  transition: width .7s var(--out);
}
.sh.show::after { width: 40px; }

/* ABOUT */
.about-g {
  display: grid;
  grid-template-columns: 180px 1fr;
  gap: 0 4rem;
  align-items: start;
}
.about-g .sh { margin-bottom: 0; }
.about-lead {
  font-family: var(--serif);
  font-size: 1.35rem;
  font-weight: 400;
  line-height: 1.55;
  margin-bottom: 1.25rem;
}
.about-body p:not(.about-lead) {
  font-size: .92rem;
  color: var(--ink-mid);
  margin-bottom: 2rem;
}
.pills { display: flex; flex-wrap: wrap; gap: .5rem; }
.pills span {
  font-size: .72rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--accent);
  background: var(--accent-pale);
  padding: .3rem .9rem;
  border-radius: 100px;
  transition: background .25s, color .25s, transform .25s var(--spring);
}
.pills span:hover { background: var(--accent); color: #fff; transform: translateY(-2px); }

/* EXPERIENCE */
#experience { border-top: 1px solid var(--ink-faint); }
.exp-item {
  display: grid;
  grid-template-columns: 200px 1fr;
  gap: 0 3rem;
  padding: 2.5rem 0;
}
.exp-meta { display: flex; flex-direction: column; gap: .4rem; padding-top: .2rem; }
.exp-date { font-size: .78rem; color: var(--ink-light); letter-spacing: .04em; }
.exp-type {
  display: inline-block;
  font-size: .68rem;
  font-weight: 500;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--accent);
  background: var(--accent-pale);
  padding: .2rem .6rem;
  border-radius: 4px;
  width: fit-content;
  transition: background .2s, color .2s;
}
.exp-type:hover { background: var(--accent); color: #fff; }
.exp-c h3 { font-family: var(--serif); font-size: 1.35rem; font-weight: 600; margin-bottom: .3rem; }
.exp-org { font-size: .82rem; color: var(--ink-light); margin-bottom: 1.25rem; }
.exp-c ul { display: flex; flex-direction: column; gap: .6rem; }
.exp-c li {
  font-size: .9rem;
  color: var(--ink-mid);
  padding-left: 1.2rem;
  position: relative;
  line-height: 1.65;
  transition: color .2s, transform .2s;
}
.exp-c li::before { content: '—'; position: absolute; left: 0; color: var(--ink-faint); font-size: .75rem; top: .15rem; transition: color .2s; }
.exp-c li:hover { color: var(--ink); transform: translateX(4px); }
.exp-c li:hover::before { color: var(--accent); }
.exp-div { height: 1px; background: var(--ink-faint); }

/* EDUCATION */
#education {
  border-top: 1px solid var(--ink-faint);
  background: var(--bg-off);
  max-width: 100%;
  padding: 5.5rem 3rem;
  scroll-margin-top: 70px;
}
#education > * { max-width: var(--maxw); margin-left: auto; margin-right: auto; }
.edu-g {
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 1.5rem;
  max-width: var(--maxw);
  margin: 0 auto;
}
.edu-card {
  background: var(--bg-card);
  border: 1px solid var(--ink-faint);
  border-radius: var(--r);
  padding: 1.75rem;
  position: relative;
  overflow: hidden;
  transition: border-color .3s, box-shadow .3s, transform .35s var(--spring);
}
.edu-card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, var(--accent), transparent);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform .4s var(--ease);
}
.edu-card:hover { border-color: var(--accent); box-shadow: 0 12px 40px rgba(181,147,106,.12); transform: translateY(-4px); }
.edu-card:hover::before { transform: scaleX(1); }
.edu-year { font-size: .72rem; letter-spacing: .1em; color: var(--ink-light); margin-bottom: .85rem; }
.edu-card h3 { font-family: var(--serif); font-size: 1.1rem; font-weight: 600; line-height: 1.3; margin-bottom: .4rem; }
.edu-deg { font-size: .85rem; color: var(--ink-mid); margin-bottom: .2rem; }
.edu-spec { font-size: .78rem; color: var(--ink-light); font-style: italic; margin-bottom: 1.25rem; }
.edu-badge {
  display: inline-block;
  font-size: .68rem; font-weight: 500;
  letter-spacing: .12em; text-transform: uppercase;
  color: var(--accent); border: 1px solid var(--accent);
  padding: .2rem .6rem; border-radius: 4px;
  transition: background .2s, color .2s;
}
.edu-badge:hover { background: var(--accent); color: #fff; }

/* VOLUNTEERING */
#volunteering { border-top: 1px solid var(--ink-faint); }

/* CERTIFICATIONS */
#certifications { border-top: 1px solid var(--ink-faint); }
.cert-list { display: flex; flex-direction: column; }
.cert-item {
  display: flex;
  align-items: flex-start;
  gap: 1.5rem;
  padding: 1.75rem 0;
  border-bottom: 1px solid var(--ink-faint);
}
.cert-item:last-child { border-bottom: none; }
.cdot {
  width: 8px; height: 8px; min-width: 8px;
  border-radius: 50%;
  background: var(--accent);
  margin-top: .45rem;
  animation: pulseGold 2.5s ease-in-out infinite;
}
.cert-item h4 { font-family: var(--serif); font-size: 1.1rem; font-weight: 600; margin-bottom: .3rem; }
.cert-item p { font-size: .88rem; color: var(--ink-mid); }

/* HONOURS */
#honours { border-top: 1px solid var(--ink-faint); }
.hdot { background: #c8963c !important; }
.hmeta { font-size: .78rem !important; color: var(--ink-light) !important; margin-bottom: .4rem !important; }

/* SKILLS */
#skills {
  border-top: 1px solid var(--ink-faint);
  background: var(--bg-off);
  max-width: 100%;
  padding: 5.5rem 3rem;
  scroll-margin-top: 70px;
}
#skills .sm, #skills .sh, #skills .skills-g { max-width: var(--maxw); margin-left: auto; margin-right: auto; display: block; }
.skills-g {
  display: grid;
  grid-template-columns: repeat(4,1fr);
  gap: 1.5rem;
}
.sk-block {
  padding: 1.75rem;
  border: 1px solid var(--ink-faint);
  border-radius: var(--r);
  background: var(--bg-card);
  position: relative;
  overflow: hidden;
  transition: border-color .3s, transform .35s var(--spring), box-shadow .3s;
}
.sk-block::before {
  content: '';
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, var(--accent), transparent);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform .4s var(--ease);
}
.sk-block:hover { border-color: var(--accent); transform: translateY(-3px); box-shadow: 0 10px 30px rgba(181,147,106,.1); }
.sk-block:hover::before { transform: scaleX(1); }
.sk-block h4 {
  font-size: .7rem; font-weight: 500;
  letter-spacing: .16em; text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 1rem; padding-bottom: .6rem;
  border-bottom: 1px solid var(--ink-faint);
}
.sk-block ul { display: flex; flex-direction: column; gap: .5rem; }
.sk-block li { font-size: .88rem; color: var(--ink-mid); font-weight: 300; transition: color .2s, transform .2s; }
.sk-block li:hover { color: var(--ink); transform: translateX(4px); }

/* HOBBIES */
#hobbies { border-top: 1px solid var(--ink-faint); }
.hob-g {
  display: grid;
  grid-template-columns: repeat(5,1fr);
  gap: 1.25rem;
}
.hob-card {
  background: var(--bg-card);
  border: 1px solid var(--ink-faint);
  border-radius: var(--r);
  padding: 2rem 1.25rem;
  text-align: center;
  transition: border-color .3s, transform .4s var(--spring), box-shadow .3s, background .3s;
}
.hob-card:hover {
  border-color: var(--accent);
  transform: translateY(-8px) scale(1.04);
  box-shadow: 0 16px 40px rgba(181,147,106,.15);
  background: var(--accent-pale);
}
.hob-icon { font-size: 2.2rem; margin-bottom: .9rem; display: block; transition: transform .4s var(--spring); }
.hob-card:hover .hob-icon { transform: scale(1.3) rotate(-6deg); animation: bobble 2s ease-in-out infinite; }
.hob-card p { font-size: .82rem; color: var(--ink-mid); line-height: 1.4; transition: color .2s; }
.hob-card:hover p { color: var(--ink); }

/* CONTACT */
#contact { border-top: 1px solid var(--ink-faint); text-align: center; padding: 7rem 3rem; }
.ct-inner { max-width: var(--maxw); margin: 0 auto; }
.ct-label { font-size: .72rem; letter-spacing: .2em; text-transform: uppercase; color: var(--accent); margin-bottom: 1rem; }
.ct-title { font-family: var(--serif); font-size: clamp(2.5rem,5vw,4.5rem); font-weight: 300; letter-spacing: -.01em; margin-bottom: 1rem; }
.ct-sub { font-size: .92rem; color: var(--ink-mid); max-width: 480px; margin: 0 auto 2.5rem; line-height: 1.7; }
.ct-btn {
  display: inline-block;
  font-size: .85rem; font-weight: 400; letter-spacing: .04em;
  color: #fff; background: var(--ink);
  padding: .95rem 2.5rem; border-radius: 100px;
  margin-bottom: 1.75rem;
  position: relative; overflow: hidden;
  transition: transform .35s var(--spring), box-shadow .3s;
}
.ct-btn::before {
  content: '';
  position: absolute; inset: 0;
  background: var(--accent);
  border-radius: 100px;
  transform: translateX(-101%);
  transition: transform .4s var(--ease);
}
.ct-btn span { position: relative; z-index: 1; }
.ct-btn:hover { transform: translateY(-3px); box-shadow: 0 12px 30px rgba(26,24,20,.18); }
.ct-btn:hover::before { transform: translateX(0); }
.ct-sec { display: flex; justify-content: center; gap: 2rem; }
.ct-sec a { font-size: .82rem; color: var(--ink-mid); border-bottom: 1px solid var(--ink-faint); padding-bottom: 1px; transition: color .25s, border-color .25s; }
.ct-sec a:hover { color: var(--accent); border-color: var(--accent); }

/* FOOTER */
footer {
  border-top: 1px solid var(--ink-faint);
  padding: 1.5rem 3rem;
  display: flex;
  justify-content: space-between;
  font-size: .75rem;
  color: var(--ink-light);
  letter-spacing: .06em;
}

/* RESPONSIVE */
@media (max-width: 900px) {
  .about-g { grid-template-columns: 1fr; }
  .edu-g { grid-template-columns: repeat(2,1fr); }
  .skills-g { grid-template-columns: repeat(2,1fr); }
  .exp-item { grid-template-columns: 1fr; gap: .5rem; }
  .exp-meta { flex-direction: row; flex-wrap: wrap; gap: .5rem; align-items: center; }
  .hob-g { grid-template-columns: repeat(3,1fr); }
}
@media (max-width: 640px) {
  #nav { padding: 1rem 1.5rem; }
  .nlinks { display: none; }
  section { padding: 3.5rem 1.5rem; }
  #education, #skills { padding: 3.5rem 1.5rem; }
  #hero { padding: 0 1.5rem 4rem; }
  .h-scroll-hint { display: none; }
  .edu-g { grid-template-columns: 1fr; }
  .skills-g { grid-template-columns: 1fr 1fr; }
  .hob-g { grid-template-columns: repeat(2,1fr); }
  footer { flex-direction: column; gap: .25rem; padding: 1.25rem 1.5rem; }
  #cdot, #cring { display: none; }
}
</style>
</head>
<body>

<!-- CURSOR -->
<div id="cdot"></div>
<div id="cring"></div>

<!-- PROGRESS BAR -->
<div id="pbar"></div>

<!-- NAV -->
<nav id="nav">
  <div class="nlogo">JR</div>
  <ul class="nlinks">
    <li><a href="#about">About</a></li>
    <li><a href="#experience">Experience</a></li>
    <li><a href="#education">Education</a></li>
    <li><a href="#volunteering">Volunteering</a></li>
    <li><a href="#certifications">Certifications</a></li>
    <li><a href="#honours">Honours</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#hobbies">Hobbies</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<!-- HERO -->
<section id="hero">
  <div>
    <img src="your-photo.jpg" class="hero-photo" alt="R. Jagathsingh Raja"/>
    <p class="h-eyebrow">Business &amp; Entrepreneurship</p>
    <h1 class="h-name">R. Jagathsingh <em>Raja</em></h1>
    <p class="h-tagline">BBA Student at IIM Bangalore &amp; Christ University &nbsp;·&nbsp; Bengaluru, India</p>
    <div class="h-links">
      <a href="/cdn-cgi/l/email-protection#15673b7f747274617d667c7b727d67747f74557278747c793b767a78"><span class="__cf_email__" data-cfemail="7705591d161016031f041e19101f05161d1637101a161e1b5914181a">[email&#160;protected]</span></a>
      <span class="hdiv">·</span>
      <a href="https://linkedin.com/in/r-jagathsingh-raja" target="_blank">LinkedIn</a>
      <span class="hdiv">·</span>
      <a href="tel:+918807498850">+91 88074 98850</a>
    </div>
  </div>
  <div class="h-line"></div>
  <div class="h-scroll-hint">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- ABOUT -->
<section id="about">
  <div class="sm reveal">01</div>
  <div class="about-g">
    <h2 class="sh reveal">About</h2>
    <div>
      <p class="about-lead reveal delay1">A driven business student with a track record of leading large-scale events, building strategic thinking skills through elite programs, and turning ideas into action.</p>
      <p class="reveal delay2" style="font-size:.92rem;color:var(--ink-mid);margin-bottom:2rem">Currently pursuing a dual path at IIM Bangalore and Christ University, I specialise in Digital Business &amp; Entrepreneurship. My experience spans event management, stakeholder coordination, and business strategy — all underpinned by a strong foundation in commerce and analytics.</p>
      <div class="pills reveal delay3">
        <span>Strategic Thinking</span>
        <span>Event Management</span>
        <span>Business Development</span>
        <span>Public Speaking</span>
      </div>
    </div>
  </div>
</section>

<!-- EXPERIENCE -->
<section id="experience">
  <div class="sm reveal">02</div>
  <h2 class="sh reveal">Experience</h2>
  <div class="exp-item reveal delay1">
    <div class="exp-meta">
      <span class="exp-date">Jun 2024 – Feb 2025</span>
      <span class="exp-type">Leadership</span>
    </div>
    <div class="exp-c">
      <h3>President of Cultural Society</h3>
      <p class="exp-org">Delhi Public School · Bengaluru, India</p>
      <ul>
        <li>Spearheaded <em>Dhanak</em>, an annual art festival with 20+ events and 800+ student participants across multiple schools — managing end-to-end planning, budgeting, and cross-functional coordination.</li>
        <li>Led school band as drummer, performing at flagship events and representing DPS at inter-school competitions.</li>
        <li>Served as Head of Delegate Affairs at DWMUN, overseeing communication and logistics for 200+ delegates.</li>
        <li>Delivered a Commerce Seminar lecture on Traditional vs. E-Marketing to an audience of 100+ students.</li>
      </ul>
    </div>
  </div>
  <div class="exp-div"></div>
  <div class="exp-item reveal delay2">
    <div class="exp-meta">
      <span class="exp-date">Aug – Sep 2023</span>
      <span class="exp-type">Internship</span>
    </div>
    <div class="exp-c">
      <h3>Event Management Intern</h3>
      <p class="exp-org">Scholastic India · Bengaluru, India</p>
      <ul>
        <li>Organised and promoted a large-scale Book Fair, developing marketing strategies to drive student engagement.</li>
        <li>Conducted interactive storytelling sessions, quizzes, and creative competitions to enhance customer experience.</li>
        <li>Contributed to sales operations and retail management, achieving measurable sales targets.</li>
        <li>Gained hands-on exposure to event planning, promotions, and stakeholder management in an educational setting.</li>
      </ul>
    </div>
  </div>
</section>

<!-- EDUCATION -->
<section id="education">
  <div class="sm reveal">03</div>
  <h2 class="sh reveal">Education</h2>
  <div class="edu-g">
    <div class="edu-card reveal delay1">
      <div class="edu-year">Aug 2025 – Sep 2028</div>
      <h3>Indian Institute of Management Bangalore</h3>
      <p class="edu-deg">Bachelor of Business Administration</p>
      <p class="edu-spec">Digital Business &amp; Entrepreneurship (DBE)</p>
      <span class="edu-badge">IIM Bangalore</span>
    </div>
    <div class="edu-card reveal delay2">
      <div class="edu-year">Jun 2025 – May 2028</div>
      <h3>Christ University, Central Campus</h3>
      <p class="edu-deg">Bachelor of Business Administration (Hons.)</p>
      <p class="edu-spec">Bengaluru, India</p>
      <span class="edu-badge">Christ University</span>
    </div>
    <div class="edu-card reveal delay3">
      <div class="edu-year">Jun 2012 – May 2025</div>
      <h3>Delhi Public School</h3>
      <p class="edu-deg">High School — Commerce Stream</p>
      <p class="edu-spec">Bengaluru, India</p>
      <span class="edu-badge">DPS</span>
    </div>
  </div>
</section>

<!-- VOLUNTEERING -->
<section id="volunteering">
  <div class="sm reveal">04</div>
  <h2 class="sh reveal">Volunteering</h2>
  <div class="exp-item reveal delay1">
    <div class="exp-meta">
      <span class="exp-date">Nov 2025 – Dec 2025</span>
      <span class="exp-type">Volunteering</span>
    </div>
    <div class="exp-c">
      <h3>Student Volunteer</h3>
      <p class="exp-org">Nightingales Medical Trust · Social Services</p>
      <ul>
        <li>Volunteered with NMT Eldercare, contributing to social service initiatives supporting elderly care over a 2-month engagement.</li>
      </ul>
    </div>
  </div>
</section>

<!-- CERTIFICATIONS -->
<section id="certifications">
  <div class="sm reveal">05</div>
  <h2 class="sh reveal">Certifications &amp; Programs</h2>
  <div class="cert-list">
    <div class="cert-item reveal delay1">
      <div class="cdot"></div>
      <div>
        <h4>McKinsey Forward Program</h4>
        <p>Comprehensive program focused on problem-solving, strategic thinking, and leadership development.</p>
      </div>
    </div>
    <div class="cert-item reveal delay2">
      <div class="cdot"></div>
      <div>
        <h4>Introduction to Strategy Consultancy — Job Simulation</h4>
        <p>Practical exposure to consulting frameworks and case-solving methodologies.</p>
      </div>
    </div>
    <div class="cert-item reveal delay3">
      <div class="cdot"></div>
      <div>
        <h4>India Sustainability Startathon 2025 Bootcamp</h4>
        <p>Innovation-focused bootcamp addressing sustainability challenges through business solutions.</p>
      </div>
    </div>
  </div>
</section>

<!-- HONOURS -->
<section id="honours">
  <div class="sm reveal">06</div>
  <h2 class="sh reveal">Honours &amp; Awards</h2>
  <div class="cert-list">
    <div class="cert-item reveal delay1" style="align-items:flex-start">
      <div class="cdot hdot"></div>
      <div>
        <h4>Joka Finlytics Battle '25 — 1st Place</h4>
        <p class="hmeta">IIM Calcutta Finance Summit &nbsp;·&nbsp; Issued by IIM Calcutta &nbsp;·&nbsp; Dec 2025</p>
        <p>Won 1st place (alongside Vivaan Arora) at the Investment Banking Case Competition organised by the Finance and Investment Club of IIM Calcutta, earning a cash prize of ₹10,000.</p>
      </div>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section id="skills">
  <div class="sm reveal">07</div>
  <h2 class="sh reveal">Skills</h2>
  <div class="skills-g">
    <div class="sk-block reveal delay1">
      <h4>Core Competencies</h4>
      <ul>
        <li>Strategic Thinking</li><li>Problem Solving</li>
        <li>Data-Driven Decision Making</li><li>Market Analysis</li><li>Business Development</li>
      </ul>
    </div>
    <div class="sk-block reveal delay2">
      <h4>Business Skills</h4>
      <ul><li>Financial Analysis</li><li>Stakeholder Management</li><li>Event Planning</li></ul>
      <h4 style="margin-top:1.5rem">Technical Tools</h4>
      <ul><li>Microsoft Excel &amp; PowerPoint</li><li>Prompt Engineering</li><li>Canva</li></ul>
    </div>
    <div class="sk-block reveal delay3">
      <h4>Soft Skills</h4>
      <ul>
        <li>Leadership</li><li>Communication</li><li>Adaptability</li>
        <li>Team Collaboration</li><li>Public Speaking</li><li>Creative Problem-Solving</li>
      </ul>
    </div>
    <div class="sk-block reveal delay4">
      <h4>Languages</h4>
      <ul>
        <li>English — Full Professional</li><li>Tamil — Native</li>
        <li>Telugu — Native</li><li>Hindi — Limited Working</li>
      </ul>
    </div>
  </div>
</section>

<!-- HOBBIES -->
<section id="hobbies">
  <div class="sm reveal">08</div>
  <h2 class="sh reveal">Hobbies</h2>
  <div class="hob-g">
    <div class="hob-card reveal delay1"><div class="hob-icon">🥁</div><p>Performing Drums</p></div>
    <div class="hob-card reveal delay2"><div class="hob-icon">⚽</div><p>Playing Football</p></div>
    <div class="hob-card reveal delay3"><div class="hob-icon">🍳</div><p>Culinary Experimentation</p></div>
    <div class="hob-card reveal delay4"><div class="hob-icon">🏍️</div><p>Biking</p></div>
    <div class="hob-card reveal delay5"><div class="hob-icon">♟️</div><p>Playing Chess</p></div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="ct-inner">
    <p class="ct-label reveal">Get in touch</p>
    <h2 class="ct-title reveal delay1">Let's Connect</h2>
    <p class="ct-sub reveal delay2">Open to internships, collaborative projects, and meaningful conversations about business and entrepreneurship.</p>
    <a href="/cdn-cgi/l/email-protection#4c3e62262d2b2d38243f25222b243e2d262d0c2b212d2520622f2321" class="ct-btn reveal delay3"><span><span class="__cf_email__" data-cfemail="fe8cd0949f999f8a968d979099968c9f949fbe99939f9792d09d9193">[email&#160;protected]</span></span></a>
    <div class="ct-sec reveal delay4">
      <a href="https://linkedin.com/in/r-jagathsingh-raja" target="_blank">LinkedIn ↗</a>
      <a href="tel:+918807498850">+91 88074 98850</a>
    </div>
  </div>
</section>

<footer class="reveal">
  <span>R. Jagathsingh Raja · 2025</span>
  <span>Bengaluru, India</span>
</footer>

<script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>
// CUSTOM CURSOR
var cdot  = document.getElementById('cdot');
var cring = document.getElementById('cring');
var mx = window.innerWidth / 2;
var my = window.innerHeight / 2;
var rx = mx, ry = my;

document.addEventListener('mousemove', function(e) {
  mx = e.clientX;
  my = e.clientY;
  cdot.style.left = mx + 'px';
  cdot.style.top  = my + 'px';
});

function animRing() {
  rx += (mx - rx) * 0.13;
  ry += (my - ry) * 0.13;
  cring.style.left = rx + 'px';
  cring.style.top  = ry + 'px';
  requestAnimationFrame(animRing);
}
animRing();

var hoverEls = document.querySelectorAll('a, .hob-card, .edu-card, .sk-block, .pills span');
hoverEls.forEach(function(el) {
  el.addEventListener('mouseenter', function() { cring.classList.add('big'); cdot.classList.add('faded'); });
  el.addEventListener('mouseleave', function() { cring.classList.remove('big'); cdot.classList.remove('faded'); });
});

// SCROLL PROGRESS BAR
var pbar = document.getElementById('pbar');
window.addEventListener('scroll', function() {
  var pct = window.scrollY / (document.body.scrollHeight - window.innerHeight) * 100;
  pbar.style.width = pct + '%';
});

// NAVBAR
var nav = document.getElementById('nav');
window.addEventListener('scroll', function() {
  if (window.scrollY > 40) nav.classList.add('scrolled');
  else nav.classList.remove('scrolled');
});

// SCROLL REVEAL
var revealEls = document.querySelectorAll('.reveal');
var revealObs = new IntersectionObserver(function(entries) {
  entries.forEach(function(entry) {
    if (entry.isIntersecting) {
      entry.target.classList.add('show');
    }
  });
}, { threshold: 0.1 });
revealEls.forEach(function(el) { revealObs.observe(el); });

// ACTIVE NAV LINK
var sections = document.querySelectorAll('section[id]');
var navLinks = document.querySelectorAll('.nlinks a');
var activeObs = new IntersectionObserver(funct